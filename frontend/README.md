AI Interview Coach — Frontend (Expo + TypeScript)

Run dev:

```bash
cd frontend
npm install
npm start
```

The app expects the backend at `http://localhost:8000` by default; update `app/services/api.ts` if needed.
